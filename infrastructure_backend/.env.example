# UPSTDC Infrastructure Backend - .env.example
# Copy to .env and adjust values per environment. Do NOT commit .env.

# Spring
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=5001

# Database (ENV-DRIVEN)
# Prefer providing discrete vars; DB_URL is still supported as override.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=upstdc_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# If provided, DB_URL takes precedence over discrete connection settings
# Format: jdbc:postgresql://<host>:<port>/<db>
DB_URL=jdbc:postgresql://localhost:5432/upstdc_dev
DB_USERNAME=postgres
DB_PASSWORD=postgres

# CORS
# Set frontend origin(s), comma-separated if multiple
CORS_ALLOWED_ORIGINS=http://localhost:3000

# JWT
JWT_SECRET=change-me-in-env-at-least-32-bytes-long-1234567890abcdef
JWT_EXPIRY_MINUTES=120

# Notes for integration:
# - Frontend must point API base URL to http://localhost:5001 (dev) unless behind reverse proxy.
# - Login endpoint: POST /api/auth/login { username, password } -> { token, username, roles }
# - Authorization: Include header Authorization: Bearer <token> for protected endpoints.
