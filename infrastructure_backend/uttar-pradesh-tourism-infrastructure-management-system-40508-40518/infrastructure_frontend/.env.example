# UPSTDC Infrastructure Frontend - .env.example
# Copy to .env and adjust as needed (Angular build-time envs may require environment.ts editing)

# Backend API base URL (dev)
NG_APP_API_BASE_URL=http://localhost:5001

# In preview deployments, this might be set to the preview backend host, e.g.:
# NG_APP_API_BASE_URL=https://<preview-backend-host>
