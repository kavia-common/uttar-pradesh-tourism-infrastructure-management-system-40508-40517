export const environment = {
  production: false,
  // PUBLIC_INTERFACE
  // Base URL for the infrastructure backend in development.
  // Update this to your dev backend host if different.
  apiBaseUrl: 'http://localhost:5001'
};
